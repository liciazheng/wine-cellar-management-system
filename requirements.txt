# The project itself runs on the standard library alone — sqlite3 ships with
# CPython, so nothing here is needed to use it. These packages are only for
# running the test suite.
#
#   python -m venv .venv
#   .venv\Scripts\python.exe -m pip install -r requirements.txt
#   .venv\Scripts\python.exe -m pytest
#
# Verified 2026-09-11 on CPython 3.13.15 (Windows 11): 83 passed.

# Test suite
pytest==9.1.1

# Transitive, pinned so the environment is reproducible
colorama==0.4.6
iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
